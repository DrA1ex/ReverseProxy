namespace ReverseProxy.BinarySerialization
{
    public enum ObjectType
    {
        Primitive,
        Nullable,
        String,
        Class,
        Struct,
        Enumerable,
        Enum,
        DateTime,
        Unsupported,
    }
}
